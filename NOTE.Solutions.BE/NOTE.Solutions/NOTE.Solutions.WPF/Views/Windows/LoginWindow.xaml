<Window x:Class="NOTE.Solutions.WPF.Views.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
        xmlns:vm="clr-namespace:NOTE.Solutions.WPF.ViewModels"
        mc:Ignorable="d"
        Title="Login Form"
        Width="600"
        Height="300"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft">



    <Grid>

        <!-- Main Layout -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- ================= LEFT SIDE (LOGO) ================= -->
        <Border Grid.Column="0" Background="#F5F6FA">
            <Image Source="/Assets/Images/logo1.png"
                   Stretch="None"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>
        </Border>

        <!-- ================= RIGHT SIDE (FORM) ================= -->
        <Grid Grid.Column="1" Margin="25">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="15"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Icon -->
            <Image Grid.Row="0"
                   Source="/Assets/Icons/icons8-lock-64.png"
                   Width="36"
                   Height="36"
                   HorizontalAlignment="Center"/>

            <!-- Username -->
            <telerik:RadWatermarkTextBox
                Grid.Row="2"
                Height="36"
                WatermarkContent="اسم المستخدم"
                Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                telerik:StyleManager.Theme="Windows8Touch"/>

            <!-- Password -->
            <telerik:RadPasswordBox
                x:Name="passwordBox"
                Grid.Row="4"
                Height="36"
                WatermarkContent="كلمة السر"
                telerik:StyleManager.Theme="Windows8Touch"/>

            <!-- Login Button -->
            <telerik:RadButton
                Grid.Row="6"
                Content="تسجيل الدخول"
                Height="36"
                FontFamily="Dubai"
                FontWeight="Bold"
                Click="loginBtn_Click"
                Style="{StaticResource centerbtn}"/>
        </Grid>
    </Grid>
</Window>
